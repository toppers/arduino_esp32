#include "diag_recorder.h"

/*
 * クラッシュ/ハング診断recorderのブート時チェック・ダンプ・初期化。
 * マスタプロセッサのみ・カーネルオブジェクト初期化済みの文脈で1回だけ
 * 呼ばれる（banner.cfgのprint_banner_copyrightと同じ「CLASSでラップしない
 * ATT_INI」＝プロセッサ非依存グループ。startup.c sta_kerのcall_inirtn
 * (&inirtnbb_table[0])がマスタプロセッサのみで実行する）。
 */
ATT_INI({ TA_NULL, 0, diag_boot_check_and_dump });
