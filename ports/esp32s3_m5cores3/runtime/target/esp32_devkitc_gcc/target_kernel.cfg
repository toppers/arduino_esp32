/*
 *  カーネル実装のコンフィギュレーションファイル（無印ESP32(Xtensa LX6)
 *  QEMU PoC用）
 *
 *  [2026-07-13新規] esp32s3_devkitc_gcc/target_kernel.cfgを雛形に作成。
 *  [2026-07-14 W0] デュアルコアSMP対応：target_ipi.cfgをINCLUDE追加。
 *  中身はchip_ipi.cfgが #if TNUM_PRCID >= 2 でガードしているため、
 *  PRC_NUM=1ビルドでは何も生成されない（シングルコア非回帰無し）。
 */
INCLUDE("target_timer.cfg");
INCLUDE("target_ipi.cfg");
