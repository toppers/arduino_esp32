/*
 *  esp_vfs_dev.h コンパイル用スタブ（W3④ BlueDroid SPP、btc_spp.c限定）
 *
 *  btc_spp.cは本ヘッダをincludeするのみで、シンボルは一切参照しない
 *  （grep確認済み。実ESP-IDFのesp_vfs_dev.hはUART等のデバイスドライバ
 *  をVFSへ登録するAPIだが、本ポートはVFSモード自体を実装しないため
 *  対応不要）。
 */
#ifndef TOPPERS_BT_STUB_ESP_VFS_DEV_H
#define TOPPERS_BT_STUB_ESP_VFS_DEV_H

#include "esp_vfs.h"

#endif /* TOPPERS_BT_STUB_ESP_VFS_DEV_H */
