/*
 *  カーネル実装のコンフィギュレーションファイル（ESP32-S3 用）
 *
 *  cross-core interrupt（IPI）はSMP対応の別作業単位で追加する
 *  （本作業単位は単一コアのみ）。
 */
INCLUDE("target_timer.cfg");
INCLUDE("target_ipi.cfg");
INCLUDE("target_diag.cfg");
