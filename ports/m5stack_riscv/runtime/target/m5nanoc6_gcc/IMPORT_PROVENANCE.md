# `target/m5nanoc6_gcc/`（M5NanoC6 target 層）の出典と改変

- 出典: asp3_esp_idf `6471444`（`~/TOPPERS/asp3_esp_idf`, branch main）の
  `asp3/target/esp32c6_espidf/`（26 ファイル）。取込み 2026-09-13。
- 参考: FMP3 側リファレンスは `fmp3/target/m5stamp_esp32p4_gcc/`（P4・4コア版）と
  `fmp3/fmp3_core/target/musca_b1_gcc/`（target_timer.cfg の CLASS 記法）。
- 監査: Task 2（`fmp3/arch/riscv_gcc/esp32c6/IMPORT_PROVENANCE.md`）と同じ方針
  （本段では pristine 監査を置かず，下表で改変を申告する）。
- 出典側との照合コマンド: `diff -u $ASP3/asp3/target/esp32c6_espidf/<f> fmp3/target/m5nanoc6_gcc/<f>`
- 補足（コメントのみの差分）: target_timer.h/target_timer.c冒頭の「TOPPERS/ASP Kernel」->
  「TOPPERS/FMP Kernel」バナー行と，esp32c6.ldの `.text` セクション先頭コメントの書き直しは，
  いずれもコードの意味を変えないコメントのみの差分である。

| ファイル | 出典との差 | 理由 |
|---|---|---|
| target_kernel.h | 全面書き直し（TNUM_PRCID=1 固定・PRC1/CLS_PRC1/CLS_ALL_PRC1・TSTEP_HRTCNT） | m5stamp_esp32p4_gcc/target_kernel.h を単一コア向けに縮めたもの（Step 2） |
| target_class.py | 新規（asp3 に該当ファイル無し） | m5stamp_esp32p4_gcc/target_class.py の TNUM_PRCID==1 の枝と同一（Step 3） |
| target_ipi.h | 新規（asp3 に該当ファイル無し） | 単一コアにつき request_dispatch_prc/request_ext_ker/request_set_hrt_event を assert(0) スタブ化（Step 4。到達 0 hit を fmp3_core/kernel で確認済み） |
| target_kernel_impl.h | 全面書き直し（FMP3 界面: target_mprc_initialize/target_initialize(PCB\*)/target_exit の宣言のみに縮小） | m5stamp_esp32p4_gcc/target_kernel_impl.h と同型（ボード kit.h は無し。Step 5） |
| target_kernel_impl.c | GPIO 診断マーク，`esp32c6_r87_apm_unblock()` 呼出し（下記参照），PMU/PLL cold 回避（TOPPERS_ESP32C6_COLD_\*），QEMU セミホスティング，Error_Handler を削除。target_initialize に p_my_pcb 引数を追加，chip_mprc_initialize()/chip_initialize(p_my_pcb) 呼出しへ変更，割込みルーティングに FROM_CPU_2/3->線20/21 を追加（asp3 は FROM_CPU_1->線18 のみ。線19 は空けている。レビュー fix round 1 で線19/20 から線20/21 へ訂正。INTNO2=線19=INTNO1+1 が fmp3_core/test/test_dcre5.h の INTNO_UNOPTED と衝突していたため） | FMP3 界面化，段1 に不要な診断コードの除去（Step 6）。**`esp32c6_r87_apm_unblock()` は「実験」ではない**（donor 側で実施87に発見，実施88で無条件化した恒久修正である。asp3 target_kernel_impl.c:100-131,278）。旧 `ESP32C6_R87_APMFIX` ガードは撤去済みで，無条件呼出しへ切替えたA/Bで独立2ブートとも冷間scanが0APから14-23APへ復帰することが実証されている（deaf-RX 修正）。本 Step 6 で落としたのは段1（未ビルド，単体リンク検証）に不要なためであり，効果を疑っているわけではない。**段4 最終レビュー是正（2026-09-15）**: 本行にかつて書いていた「seam では bootloader の bootloader_init_mem() が APM フィルタを無効化するので不要になる見込み」は**誤り**であった（段4 Task 5 が実機で反証）。esp-idf `components/bootloader_support/src/bootloader_mem.c:17-37` の `apm_hal_enable_ctrl_filter_all(false)` は `#if !defined(BOOTLOADER_BUILD)` の中＝**アプリ側の cpu_start だけ**が行い、bootloader は行わない。seam はアプリ側 cpu_start をリンクしないので POR 既定（フィルタ有効）のまま modem の DMA が HP SRAM に届かず scan 0 AP になった（`HP_APM M1 status=1 info0=0x00130001`。CPU は TEE なので CPU 例外は出ず、modem 側のラッチにしか残らない）。IDF 同等の解除（3 コントローラの `FUNC_CTRL` を 0、asp3 の TEE 昇格は行わない）は現在 `esp/shim/esp_wifi_adapter.c` の `c6_apm_unblock()`（:1154-1170、`A1_C6_APM_UNBLOCK` 既定 ON、OFF が「外した対照」= 0 AP）に在り、`wifi_clock_enable_wrapper` の初回で呼ぶ。**BLE 等で modem を使う構成を足す前に、本 target_kernel_impl.c（system 起動時の処理として）へ移す予定**（hello の sha が動くので非回帰を採り直す）。出典: `.steering/20260913-c6-fmp3-wifi-plan/PLAN-stage4-impl.md` の「F5 訂正」行、`.steering/20260913-c6-stage4/README.md` 5-2 節、`esp/shim/IMPORT_PROVENANCE_c6.md` 10 節。super WDT 解錠キーは asp3 のバグ修正コメント通り `0x50D83AA1` を採用。当時（本行の元記述時点）は本 repo の `esp32c6.h` の `ESP32C6_LP_WDT_SWD_WKEY` が誤値 `0x8F1D312A` のままで、target_kernel_impl.c はマクロを使わず直値 `0x50D83AA1U` を書いて回避していた。**段4 fix wave 1 (E, 2026-09-14) でこの食い違いを解消**：`esp32c6.h` の `ESP32C6_LP_WDT_SWD_WKEY` を asp3/lpwdt_ll.h:30 の `LP_WDT_SWD_WKEY_VALUE` と同じ `0x50D83AA1` へ訂正し、target_kernel_impl.c も直値ではなくマクロ `ESP32C6_RTC_CNTL_SWD_WKEY` を参照する形へ統一した（値は不変のため生成コードは無改変。objdump で確認済み、詳細は `.superpowers/sdd/PLAN-stage4-impl/fixwave-1-report.md` E節） |
| target_timer.h | INTNO_TIMER/INHNO_TIMER_PRC1/INTPRI_TIMER/INTATR_TIMER を FMP3 形へ再定義。target_hrt_set_event(HRTCNT)->target_hrt_set_event(ID prcid, HRTCNT)，target_hrt_raise_event(void)->target_hrt_raise_event(ID prcid)，target_hrt_clear_event(ID prcid) を新設。診断用 extern（esp32c6_hrt_set_event_\* 3本・g_hrt_last_target）を削除 | FMP3 の HRT 界面（musca_b1_gcc/target_timer.h 参照。Step 7） |
| target_timer.c | 診断用カウンタ実体（esp32c6_hrt_set_event_\*・g_hrt_last_target）と target_hrt_handler 内の RTC RAM 計測（rc[12..14]）を削除。target_hrt_initialize/target_hrt_terminate/target_hrt_handler の3関数のみに縮小 | 同上（Step 7） |
| target_timer.cfg | `#ifdef USE_TIM_AS_HRT` ガードを除去し `CLASS(CLS_PRC1) { ... }` 形式へ書き直し。ATT_INI/ATT_TER の関数名を `_kernel_target_hrt_initialize`/`_kernel_target_hrt_terminate`（genrename後の名前）に変更，DEF_INH を `INHNO_TIMER_PRC1` へ変更。**2026-09-13 Task 5 で追記修正**: DEF_INH の第2要素を `target_hrt_handler`（生の名前）から `_kernel_target_hrt_handler`（genrename後の名前）へ変更 | musca_b1_gcc/target_timer.cfg と同じ FMP3 CLASS 記法（Step 7）。Task 5 の修正理由: musca_b1_gcc は target_rename.def に `target_hrt_handler` を含めていないため生の名前のままでよいが，本ターゲットの target_rename.def（Step 11）は `target_hrt_handler` も rename 対象に含めている。kernel_cfg.c は `kernel_int.h`→`kernel_unrename.h`→`target_unrename.h` 経由で `#undef target_hrt_handler` が効いた状態でコンパイルされるため，ATT_INI/ATT_TER と同じく genrename 後の名前を直接書く必要があった（実装計画 Task 5 のビルドで `'target_hrt_handler' undeclared` として発覚） |
| target_serial.cfg | 変更なし | 複製のまま（`INCLUDE("chip_serial.cfg")`）。chip 側 chip_serial.cfg を Task 3 で CLASS(CLS_SERIAL) 形へ書き換えた（Step 8） |
| target_syssvc.h | TARGET_NAME を `"ESP32-C6"`->`"M5NanoC6 (ESP32-C6)"` へ変更。`#ifdef TOPPERS_OMIT_TECS` ブロックへ `CLS_SERIAL`（=CLS_PRC1）・`ISR_SIO`（=1）を追加 | m5stamp_esp32p4_gcc/target_syssvc.h と同型（Step 8） |
| target_test.h | 全面書き直し（asp3 は INTNO1 のみ）。INTNO1/2/3（線18/20/21）と intno1_clear/intno2_clear/intno3_clear を定義 | FMP3 の core_test.h が INTNO1/2/3 の3本を要求するため。intno\*_clear は controller ruling によりアドレス算術（FROM_CPU_0+n\*4U）ではなく named macro（ESP32C6_INTPRI_CPU_INTR_FROM_CPU_1/_2/_3）を使用（Step 9）。線19（INTNO1+1）は fmp3_core/test/test_dcre5.h の INTNO_UNOPTED と衝突するため空け，INTNO2/3 を線20/21 へ割り当て直した（レビュー fix round 1。当初は線19/20 を割り当てていた）。**段3 Task 4 Step 0（2026-09-14）**: INTNO1_INTPRI を asp3 由来の `(-2)` から `TMAX_INTPRI` へ変更（P4/S3 と同じ値）。`fmp3/test_ovr/test_ipmmask.c` が `chg_ipm(-1)` で INTNO1 をマスクする前提を持つため，`(-2)` のままでは IPM=-1 で正当に未マスクとなり test_ipmmask が FAIL していた（intmtx の欠陥ではなくテスト前提とtarget値の不一致，controller ルーリング）。INTNO2/3 の `(-4)` は変更なし |
| esp32c6.ld | ENTRY を `_flash_entry`->`toppers_start` へ変更。`.text` 先頭の Direct Boot ヘッダ用 KEEP 行（マジックナンバー8バイト分）と `KEEP(*(.text.entry))` の2行を削除。冒頭コメントに段1限定・出典・段2での作り直し予定を追記（残りは出典コメントを verbatim 引用）。**2026-09-13 Task 5 で追記**: `.bss` 出力セクション内に `__sbss_start`/`__sbss_end` シンボルを追加（境界を `__bss_start` と同じ位置に置き，sbss範囲は空とする）。**最終レビュー是正で追記**: `.kernel_data_CLS_*`（FMP3 のクラス別 PCB/TCB/SEMCB 等，初期値つき）と `.stack_CLS_*`（クラス別スタック）に入力規則が無く，orphan として `.data` の直後に LMA=FLASH の PROGBITS で配置され，`__idata_start..__idata_end` のコピー範囲にも `.bss` のゼロ初期化にも入っていなかった（未初期化のまま実行される欠陥。P4 の esp32p4_xip.ld:232-268 と同型）。`.kernel_data_CLS_*` を `.data` 出力セクションへ，`.stack_CLS_*` を `.bss`（NOLOAD）出力セクションへ明示的に取り込んで解消した | FMP3 共通 start.S のエントリへ合わせる（Step 10）。MEMORY の RAM ORIGIN/LENGTH は asp3 側の診断用シフト値（0x40819000/412k）のまま--本 Task の指示は上記3点のみで RAM 配置の是正は含まれない。段2（seam 配置）で作り直す。Task 5 追記の理由: fmp3_core 共通 start.S（差替え後は chip_start.S）が sbss と bss を別々にゼロクリアするため両シンボルを要求するが，出典 asp3 の crt0 は分けてクリアしないためこれらのシンボル自体が出典 ld に無く，実装計画 Task 5 のビルドでリンクエラー（`undefined reference to __sbss_start`）として発覚した。orphan 修正は最終レビューで発見，是正（実測: 修正前は section header に独立した `.kernel_data_CLS_PRC1`(0x150)/`.stack_CLS_PRC1`(0x1800) が存在し `_kernel_pcb_prc1` が `__idata_end` の外側にあった。修正後は両セクションが `.data`/`.bss` に吸収され `nm -u` は空のまま） |
| target_rename.def | `target_mprc_initialize` を追加。`# target_timer.c` セクションを新設し `target_hrt_initialize`/`target_hrt_terminate`/`target_hrt_handler` を追加 | target_kernel_impl.c/target_timer.c の新規公開関数を genrename 対象に（Step 11） |
| target_rename.h / target_unrename.h | 全面差し替え（genrename.rb で再生成。ASP3 版を持ち込んでいない） | `ruby fmp3_core/utils/genrename.rb target` で再生成。`grep -c "target_hrt_handler\|target_mprc_initialize" target_rename.h` = 2 を確認済み（Step 11） |
| target_stddef.h | `TOPPERS_ESP32C6_GCC`->`TOPPERS_M5NANOC6_GCC` | システム略称をターゲット名に合わせた（Step 12） |
| target.cmake | 全面書き直し（asp3 版 590 行から段1 に要る部分だけを抽出。Wi-Fi/BT/lwIP/PMU/cold-boot の option は持ち込まない）。**2026-09-13 Task 5 で追記**: FMP3_INCLUDE_DIRS へ `esp/config/esp32c6/hal_stub_include`（新規）を追加 | fmp3_core が FMP3_TARGET_DIR として include する契約（m5stamp_esp32p4_gcc/target.cmake と同じ分担。Step 13）。Task 5 追記の理由: `esp/config/esp32c6/sdkconfig.h`（NuttX向け生成物の verbatim コピー）が無条件に `#include <nuttx/config.h>` するため（実装計画 Task 5 のビルドで `fatal error: nuttx/config.h: No such file or directory` として発覚）。esp32(LX6)/esp32s3 の `esp/config/esp32/hal_stub_include` と同じ役割・同じ配置規約でスタブを新設した |
| target_asm.inc / target_cfg1_out.h / target_check.py / target_kernel.cfg / target_kernel.py / target_serial.h / target_sil.h（計7本） | バイト同一（`cmp` で確認済み） | - |
| esp32c6_usbjtag_hal.c（**段2・2026-09-14 追加修正、同日レビューで再修正、同日 段3 Task 1 で計測カウンタ追加と IN_EMPTY 修正**） | 取込み時点（Step 1）は asp3 とバイト同一だったが，段2 の実機評価で乖離した。`esp32c6_usbjtag_opn_por` にバスリセット割込み（`USB_SERIAL_JTAG_INTR_BUS_RESET`）の恒常有効化を追加し，`esp32c6_usbjtag_isr_siop` にそのハンドラを追加した。**再修正**: `opn_por` は割込みを clr してから ena するため，seam エントリから `serial_opn_por` 呼出しまでの数 ms の間に起きたバスリセットは discard され，次のバスリセットが来るまで未送信パケットが再武装されない窓があった。`opn_por` の ena 直後にも同じ再武装チェック（`txfifo_writable()==0` なら `txfifo_flush()`）を追加して窓を閉じた。**段3 Task 1**: `#ifdef ESP32C6_USJ_PROBE`（既定 OFF、`A1_C6_USJ_PROBE` で有効化）で `esp32c6_usj_cnt[5]`（ISR で IN_EMPTY を受けた回数／そのうち txfifo not writable だった回数／`ena_cbr(SIO_RDY_SND)` 時点で int_raw が既に立っていた回数／`snd_chr` が false を返した回数／`isr_sndfail`＝irdy_snd 呼出し中に限定した snd_chr 失敗数）を追加。当初 notwritable（cnt[1]）だけでは AC-1a（drops との 1:1）が確認できなかったが，controller ルーリングで isr_sndfail（cnt[4]）を追加したところ厳密な 1:1 が確認できた（README.md §4-2）ため，IN_EMPTY 分岐を `txfifo_writable() != 0` のときだけ `irdy_snd` を呼ぶよう修正した。`#ifdef ESP32C6_USJ_NOFIX`（既定 OFF、`A1_C6_USJ_NOFIX` で有効化）で修正前の無条件呼出しへ戻せる（positive control 用、README.md §4-7 で外す対照を実演済み） | ホスト不在（USBケーブル未接続のホスト，または列挙前）で FMP3 が起動し USJ の EP1 へ書込んで WR_DONE すると，`SERIAL_IN_EP_DATA_FREE` が 0 のまま固まり，後にホストが列挙してもエンドポイントが再武装されず，logtask が割込み駆動送信で永久にブロックして出力が一切届かない欠陥を実機で 6/6 再現した（真cold 検証中に発見，`.steering/20260913-c6-stage2/README.md` §7 参照）。IDF のドライバ（`esp-idf/components/esp_driver_usb_serial_jtag/src/usb_serial_jtag.c`）は送信完了時に未送信分が残っていれば WR_DONE を再発行する設計だが，バスリセット契機での再武装は持たない。本ドライバも元々バスリセットのタイミングでの再武装を持っていなかった（donor の asp3 esp32c6_usbjtag.c にも同種の対応は無い）。asp3 側への還元は本タスクの範囲外。段3 Task 1 の計測カウンタと IN_EMPTY writable チェックも asp3 への還元対象外（計測専用カウンタと，本ドライバ固有の stale IN_EMPTY 事情への対処のため） |
| （持ち込まない）Direct Boot ヘッダ生成用アセンブリ（asp3 側の該当ソース） | - | seam では bootloader が渡すため Direct Boot 専用のマジックヘッダは不要 |
| （持ち込まない）run.cmake | - | classic (configure.rb+Makefile) 系のランナーで，本 repo は CMake preset 系（本ファイル「ビルドフロー」節参照） |
| （持ち込まない）target_os_awareness.py | - | 本 repo に対応機構が無い（Task 2 の chip_os_awareness.py と同じ扱い） |
| esp32c6_xip.ld（**Task 3・2026-09-13 新規**） | 新規（asp3 に該当ファイル無し） | 段2 seam（実 ESP-IDF 2nd-stage bootloader 起動）用リンカスクリプト。出典は 3 つの合成: (1) 本ディレクトリの esp32c6.ld（RAM 側 .data/.bss 入力規則を verbatim 流用）、(2) esp-idf v5.5.4 `components/esp_system/ld/esp32c6/sections.ld.in` の `.flash_rodata_dummy` 方式（317-323行）、(3) `fmp3/target/m5stamp_esp32p4_gcc/esp32p4_xip.ld` の設計メモ（(2)(6)）。**段1 から意図的に変えた点**: `.data` に `AT()` を付けない（LMA==VMA）。段1 の `AT(LOADADDR(.text)+SIZEOF(.text))` は Direct Boot（フラット生バイナリ）専用の手法で、esp_image 形式では esptool の既定動作（`bin_image.py:1185` `image.segments = e.sections`、ELF セクションの sh_addr=VMA を使う）により無意味・有害（詳細は本ファイルの冒頭コメントおよび `.steering/20260913-c6-stage2/README.md` §3）。`cmake/a1_c6_seam_image.sh` の C-6 検査（RAM セグメント内容と ELF `.data` の実バイト列が一致すること）で実測により裏を取った |
| app/fmp_app/fmp_app.c / fmp_app.h / fmp_app.cfg（**Task 3・2026-09-13 新規**） | fmp_app.h/fmp_app.cfg はコメントのみ差分の複製（`fmp3/target/m5stamp_esp32p4_gcc/tools/fmp_app/` より。`diff` で確認済み、コード行は無改変）。fmp_app.c は P4 版に PCR 読出し syslog 行 1 本を追加した以外は同一 | seam 段2 の既定アプリ（D1 の証拠として起動直後に PCR_SYSCLK_CONF/PCR_CPU_FREQ_CONF を読んで syslog する）。fmp_app.cfg の `#if TNUM_PRCID >= 2` ガードは複製元（P4 版）に既にあり、本 target（TNUM_PRCID=1 固定）ではそのまま無効化されるため改変不要だった |
| target_timer.h（**段4 Task 3・2026-09-14 追記、同日 minor round で実体を target_hrt64.c へ**） | `target_hrt_get_current64()` をファイル末尾に**マクロ**として追加。実体は `target_hrt64.c` の `esp32c6_systimer_read64_us()`（extern 宣言のみ本ヘッダ）。minor round 前は `esp32c6_systimer_read()`（hi -> lo を 1 回ずつ）を割るマクロだったが、タスク文脈/Wi-Fi ISR から呼ぶと hi と lo の間の別 snapshot で lo ラップ跨ぎ（268 秒ごと）に 2^32 ずれるため、esp-idf `hal/systimer_hal.c:46-68` と同じ lo/hi/lo 再読ループ版へ | `esp/shim/esp_shim.c` の `esp_shim_time_us()`（単調増加 64bit us）が呼ぶ。S3/LX6 は CCOUNT の 64bit 累積器を Inline 関数で持つが、C6 の SYSTIMER は 52bit フリーランなので割るだけでよい。Inline 関数にしなかった理由（実測）: 本ヘッダはカーネル全 TU に入り、未使用でも GCC の関数番号（.LFBnnn）が 1 ずれて .symtab/.debug_loclists が変わり、ロード可能セクション不変のまま ELF sha256 が変わる。seam の app_desc は ELF 全体の sha256（app_elf_sha256）を持つので非 wifi の hello の app_xip.bin まで変わる（65 バイト差＝app_elf_sha256 32B + 末尾 SHA256 32B + checksum 1B を確認）。マクロなら関数番号に影響せず、hello の app_xip.bin は fix wave 1 の値（`20042237...`）と一致する |
| esp32c6_xip.ld（**段4 Task 3・2026-09-14 追記**） | `.text` に在った `*(.iram1 .iram1.*)` を `.data`（RAM）へ移し、blob の IRAM 群（`.wifi0iram/.wifirxiram/.wifiextrairam/.wifiorslpiram/.wifislpiram/.wifislprxiram/.coexiram/.coexsleepiram/.phyiram`）を同じ場所に加えた | 計画 F4（IRAM 群を RAM 常駐に。S3 と同型）。入力セクション名の出典は esp-idf の linker fragment（`esp_wifi/linker.lf`、`esp_coex/linker.lf`、`esp_phy/linker.lf`）と `esp_attr.h` の `IRAM_ATTR`。`.data` の中に置くのは `cmake/a1_c6_seam_image.sh` の C-6（RAM セグメント全部を ELF `.data` と比較）を無改変で通すため（AC-3e）。非 wifi の hello には `.iram1` 入力が無く（`readelf -S` でセクション配置・サイズ不変、app_xip.bin sha256 不変を実測）影響しない |
| diag_recorder.h / diag_recorder.c（**段4 Task 3・2026-09-14 新規**） | 新規（asp3 に該当ファイル無し）。API（`diag_event/diag_heartbeat/diag_panic_snapshot/diag_boot_check_and_dump` とイベント ID）は `fmp3/target/esp32s3_devkitc_gcc/diag_recorder.h` と同一。実装は S3 版（同 `diag_recorder.c:44-56, 212-246`）の縮小版: 64 エントリのリングを通常 .bss に置き、リセット跨ぎの永続化（`.diag_noinit`・checksum）・Xtensa SFR スナップショット・ATT_INI での前回ダンプは持たない（Low#3-5、ヘッダ冒頭に明記）。ts は SYSTIMER 下位 32bit | `esp/shim/esp_shim.c`・`esp_shim_isr_ctx.c`・`esp/wifi/net/netif_esp32s3.c` が無条件に `#include "diag_recorder.h"` し `diag_event()` を呼ぶため（S3/LX6 では target ディレクトリが持つ）。`cmake/a1_c6_stage1.cmake` の `A1_C6_WIFI` ブロックだけがリンクする（hello には入らない） |
| target_hrt64.c（**段4 Task 3 minor round・2026-09-14 新規**） | 新規（asp3 に該当ファイル無し）。`esp32c6_systimer_read64()`（lo/hi/lo の再読ループ、esp-idf `components/hal/systimer_hal.c:46-68` `systimer_hal_get_counter_value()` と同形）と `esp32c6_systimer_read64_us()` | `target_hrt_get_current64()` の実体。別 TU にしたのは target_timer.h に Inline 関数を足すとカーネル全 TU の GCC 関数番号がずれ hello の ELF sha256（app_desc）が変わるため（soft な理由。hello は preset golden ではない）。`cmake/a1_c6_stage1.cmake` の `A1_C6_WIFI` ブロックだけがリンク |
