/*
 *  TOPPERS/FMP3 ESP32-P4 移植 —
 *  SDIO ホストドライバが使うカーネルオブジェクト
 *
 *  Copyright (C) 2026 by Embedded and Real-Time Systems Laboratory
 *              Graduate School of Information Science, Nagoya Univ., JAPAN
 *
 *  上記著作権者は，本ソフトウェアをTOPPERSライセンス（条件は他のソー
 *  スファイルの先頭コメントを参照）の下で利用することを許諾する．本ソ
 *  フトウェアは無保証で提供される．
 *
 *  移植元（P4 repo `wifi_p4_module/os_adapter/fmp3_hosted_osi.cfg:31-35`）は
 *  ここで `CFG_INT(INTNO_SDMMC)`/`CRE_ISR` も宣言していたが、本 repo では
 *  **割込み線は C-1 CLIC シムの表（`esp_shim_intr_clic.cfg`）にしか書かない**
 *  （改変 D-3。利用者ごとに線を写すと必ず食い違う）。
 *
 *  ISR は `esp_shim_clic_intr_isr` を経由して `p4sdio_isr` へ届く。
 *  ⇒ 本ファイルはセマフォ 2 本とミューテックス 1 本だけを作る。
 */

CLASS(CLS_PRC1) {
	/*  コマンド/転送完了イベント（ISR→isig_sem。初期 0・最大 1）  */
	CRE_SEM(P4SDIO_SEM_EVT, { TA_NULL, 0, 1 });
	/*  SDIO カード（スレーブ）割込み（ISR→isig_sem。初期 0・最大 1）  */
	CRE_SEM(P4SDIO_SEM_IO,  { TA_NULL, 0, 1 });
	/*  トランザクション排他  */
	CRE_MTX(P4SDIO_MTX, { TA_NULL, 0 });
}
