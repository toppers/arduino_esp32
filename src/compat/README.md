# Compatibility layer

This directory is reserved for the minimum compatibility APIs required by Arduino-ESP32,
M5Unified, and M5GFX.

Implementations must be compared with the existing `esp32_s3/esp/shim` and
`esp32_s3/m5/compat/freertos` code before new wrappers are added.
